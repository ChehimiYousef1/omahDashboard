# OMAH Connect Admin frontend configuration
#
# Variables beginning with VITE_ are compiled into the frontend bundle and
# therefore MUST NOT contain passwords, tokens, API secrets, or private keys.

VITE_API_URL=http://localhost:5000/api
